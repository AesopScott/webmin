{
  "name": "webmin",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build",
    "deploy": "npm run build && firebase deploy",
    "dev": "cd client && npm run dev",
    "emulate": "firebase emulators:start",
    "setup": "npm install && cd client && npm install && cd ../functions && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
